<p>Global session attribute direct reference: ${(globalSessionAttr1)!'?'}</p>
<p>Global session attribute direct reference: ${(globalSessionAttr2)!'?'}</p>
<p>Global sessions attributes got via method-level model attrs: ${(sa)!'?'}                </p>
<p>Request Attribute added via interceptor - direct reference: ${(start)!'?'}             </p>
<p>Request Attribute added via interceptor got via model attr:${(ra)!'?'}             </p>
<p>Global model attribute added via interceptor: ${(duration)!'?'}             </p>
